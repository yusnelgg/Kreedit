version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=./kreedit.db
    volumes:
      - ./kreedit.db:/app/kreedit.db
    restart: unless-stopped